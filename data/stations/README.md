# Station Data

Place only redistributable station metadata/import descriptors here.

Expected fields include stable id, name/aliases, coordinates, type, river/basin relationship, timezone, datum, source and active state.

Production observations/time series do not belong in Git.
