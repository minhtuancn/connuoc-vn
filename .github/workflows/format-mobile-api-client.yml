name: Format mobile API client

on:
  push:
    branches:
      - phase-3/mobile-api-client

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: phase-3/mobile-api-client
          fetch-depth: 0
      - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: '3.47.3'
          channel: stable
          cache: true
      - name: Format Dart sources
        working-directory: apps/mobile
        run: dart format lib test
      - name: Commit formatter output
        shell: bash
        run: |
          set -euo pipefail
          git add apps/mobile/lib apps/mobile/test
          if git diff --cached --quiet; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'style(mobile): apply Dart formatter'
          git push origin HEAD:phase-3/mobile-api-client
