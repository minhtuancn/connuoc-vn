name: Format mobile cache tests

on:
  push:
    branches:
      - phase-3/mobile-offline-persistence

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mobile
    steps:
      - uses: actions/checkout@v5
        with:
          ref: phase-3/mobile-offline-persistence
          fetch-depth: 0

      - name: Setup Flutter 3.47.3
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: '3.47.3'
          channel: stable
          cache: true

      - name: Format Task 2 tests
        run: dart format test/core/database/cache_key_test.dart test/core/database/cache_freshness_test.dart

      - name: Commit formatted tests
        working-directory: .
        shell: bash
        run: |
          set -euo pipefail
          git add apps/mobile/test/core/database/cache_key_test.dart \
            apps/mobile/test/core/database/cache_freshness_test.dart
          if git diff --cached --quiet; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'style(mobile): format cache contract tests'
          git push origin HEAD:phase-3/mobile-offline-persistence
